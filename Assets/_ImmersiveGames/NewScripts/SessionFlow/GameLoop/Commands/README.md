# GameLoop Commands

Facade legada temporaria para `Pause`/`Resume`.

`Victory`/`Defeat` pertencem ao Run Pipeline.
`ExitToMenu` pertence a Route/SessionOperational.
Este modulo nao deve ser tratado como command surface global do GameLoop.
